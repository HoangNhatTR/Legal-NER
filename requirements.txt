# Vietnamese Legal NER — inference bundle runtime dependencies.
# Versions pinned to the environment the production model was trained/served on.
# Install with:  pip install -r requirements.txt
#
# NOTE on torch: the pin below installs the default (CUDA-enabled on Linux)
# build. The bundle also runs fine on CPU. If you want a CPU-only / specific
# CUDA build, install torch first from https://pytorch.org then run pip again.

# --- model + inference ---
torch==2.12.0
transformers==5.8.1
tokenizers==0.22.2
safetensors==0.7.0
numpy==2.3.5

# --- PDF text extraction (default extractor, pure-Python, no Java) ---
PyMuPDF==1.27.2.3

# --- HTTP API (/extract, /health, /jobs/extract) ---
fastapi==0.136.1
uvicorn==0.47.0
pydantic==2.13.4
python-multipart==0.0.29

# --- OPTIONAL: opendataloader-pdf extractor (?extractor=opendataloader) ---
# Requires a JDK 11+ on the host. Uncomment to enable; the default pymupdf
# extractor needs none of this and runs everywhere out of the box.
# opendataloader-pdf>=0.1
